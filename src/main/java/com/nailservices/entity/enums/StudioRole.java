package com.nailservices.entity.enums;

public enum StudioRole {
    OWNER,      // Can manage everything in the studio
    ADMIN,      // Can manage services and providers
    PROVIDER    // Can only manage their own schedule
}
