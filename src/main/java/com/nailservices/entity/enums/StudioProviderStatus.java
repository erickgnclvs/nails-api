package com.nailservices.entity.enums;

public enum StudioProviderStatus {
    ACTIVE,     // Provider is actively working in the studio
    PENDING,    // Invitation sent but not yet accepted
    INACTIVE    // Provider is no longer active in the studio
}
